name: Docker

on:
  pull_request:
  push:
    branches:    
      - master
      - github-actions

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Docker Build
        run: docker-compose build
